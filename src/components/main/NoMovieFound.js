import React from 'react';

const NoMovieFound = () => {
    return (
        <div>
            <h1> Sorry, we don't have such movie in our DB</h1>
        </div>
    );
};

export default NoMovieFound;
